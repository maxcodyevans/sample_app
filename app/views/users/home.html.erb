

  <%= form_tag(root_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append" align="right">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Articles" %>
      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn" type="submit"><i class="icon-search"></i></button>
    </div>
  <% end %>



<table>
  <thead>
    <tr>
      <th>Profile</th>
      <th>Name</th>
      <th>Subjects</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
<%= will_paginate %>
  <tbody>
    <% @users.each do |user| %>
      <tr>
      	<td><section class="pictureround"><%= link_to gravatar_for( user, size:150), user %></section></td>
        <td><%= user.name %></br> </br> I teach 
        <%= user.tag_list %> ... </td>
   		<% if user.microposts.any? %>
    		 <td><%= user.microposts.first.content %></td>
    	<% end %>
  

      </tr>
    <% end %>
  </tbody>

</table>


<%= link_to 'New User', new_user_path %>
